# book-data-json-to-sqlite
A Python tool that converts a Ruby-style hash JSON file into valid JSON, loads it into SQLite, normalizes price values, generates analytical summary tables, and exports results as plain text.
